import React from 'react'
import {render, fireEvent, waitFor, screen} from '@testing-library/react'
import App from './App'

describe('', () => {
    test('should ', () => {
   render(<App/>);
expect(screen.getByText("curtis")).toHaveTextContent("curtis");

   })
})

//test to see if model appears

//test if delete button works 
